/* const { createUser, login } = require("./db");

createUser({
    email: "brad@gmail.com",
    first_name: "Brad",
    last_name: "P",
    password: "brad",
}).then((newUser) => {
    console.log("newUser", newUser);
}); */
